<h1>Welcome to Bathroom Ai</h1>

<% @rooms.each do |room| %>
  <div>
    <span> <%= room.name %></span>
    <span class="status <%= occupied_class(room.occupied) %>"</span>
    <span><%= occupied_message(room.occupied) %></span>
  </div>
<% end %>
<%= @cookies %>

<script>
window.onload = function() {
  setTimeout(function(){
  window.location.reload();
  console.log("Reload")
}, 1000);
};
</script>

